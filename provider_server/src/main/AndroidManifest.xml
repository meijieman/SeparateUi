<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.baidu.provider.server">

    <application>
        <!-- 跨进程 -->
        <service
            android:name="com.baidu.provider.server.ConnService"
            android:exported="true">
            <intent-filter>
                <action android:name="com_baidu_provider_conn_service" />
            </intent-filter>
        </service>
    </application>

</manifest>